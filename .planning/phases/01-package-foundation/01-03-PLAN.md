---
phase: 01-package-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - package.json
  - scripts/validate-metadata.mjs
  - scripts/validate-docs.mjs
  - scripts/validate-phase-01.mjs
  - .planning/phases/01-package-foundation/01-VALIDATION-RUN.json
  - .planning/phases/01-package-foundation/01-VALIDATION.md
autonomous: true
requirements:
  - DIST-01
  - DIST-02
user_setup: []
must_haves:
  truths:
    - "`npm run validate:metadata` verifies exact package metadata, skill frontmatter, and the unique Phase 1 marker instead of only checking that a script name exists."
    - "`npm run validate:docs` verifies exact README and INSTALL command coverage plus the package-specific smoke marker expectations instead of only checking that docs files exist."
    - "`npm run validate:phase-01` verifies the packed tarball rather than trusting the source tree and records the validated unpack/install paths for downstream smoke use."
    - "The validation flow proves that the packaged artifact contains every runtime resource needed for Pi skill discovery and that a clean local `pi install -l` flow from the unpacked tarball succeeds."
    - "The local smoke flow verifies output containing `PHASE_1_PACKAGE_MARKER: pi-code-reviewer`, so it cannot false-pass on a different installed `code-reviewer` skill."
  artifacts:
    - path: "package.json"
      provides: "Repeatable validation entrypoints for Phase 1"
      contains: '"validate:phase-01": "node scripts/validate-phase-01.mjs"'
    - path: "scripts/validate-metadata.mjs"
      provides: "Exact manifest and skill-marker validation"
      min_lines: 25
    - path: "scripts/validate-docs.mjs"
      provides: "Exact README and INSTALL validation"
      min_lines: 25
    - path: "scripts/validate-phase-01.mjs"
      provides: "Tarball inspection, local Pi install verification, and handoff artifact generation"
      min_lines: 60
    - path: ".planning/phases/01-package-foundation/01-VALIDATION-RUN.json"
      provides: "Recorded unpacked package and clean temp-project paths for Task 3 smoke reuse"
      contains: '"testProject"'
    - path: ".planning/phases/01-package-foundation/01-VALIDATION.md"
      provides: "Updated Phase 1 validation contract with concrete commands and latest smoke result capture"
      contains: "PHASE_1_PACKAGE_MARKER: pi-code-reviewer"
  key_links:
    - from: "package.json"
      to: "scripts/validate-metadata.mjs"
      via: "npm script `validate:metadata` executes the exact metadata validator"
    - from: "package.json"
      to: "scripts/validate-docs.mjs"
      via: "npm script `validate:docs` executes the exact docs validator"
    - from: "package.json"
      to: "scripts/validate-phase-01.mjs"
      via: "npm script `validate:phase-01` executes the full tarball/install validator"
    - from: "scripts/validate-phase-01.mjs"
      to: ".planning/phases/01-package-foundation/01-VALIDATION-RUN.json"
      via: "validator writes the exact `testProject` and `packageDir` handoff needed for Task 3 smoke reuse"
    - from: ".planning/phases/01-package-foundation/01-VALIDATION.md"
      to: ".planning/phases/01-package-foundation/01-VALIDATION-RUN.json"
      via: "validation doc tells executors how to reuse the recorded temp-project artifact for marker-based smoke testing"
---

<objective>
Verify that the packed/installable artifact contains every runtime resource needed for Phase 1 install and discovery.

Purpose: Convert the scaffold and docs into a repeatable artifact-first local validation flow so Phase 1 is not marked complete based only on repository files. Real npm publication and registry-install completion are deferred to the final release phase.
Output: Exact validation scripts in `package.json`, `scripts/validate-metadata.mjs`, `scripts/validate-docs.mjs`, `scripts/validate-phase-01.mjs`, the handoff artifact `.planning/phases/01-package-foundation/01-VALIDATION-RUN.json`, and an updated `.planning/phases/01-package-foundation/01-VALIDATION.md`.
</objective>

<execution_context>
@.pi/gsd/workflows/execute-plan.md
@.pi/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-package-foundation/01-CONTEXT.md
@.planning/phases/01-package-foundation/01-RESEARCH.md
@.planning/phases/01-package-foundation/01-VALIDATION.md
@package.json
@README.md
@skills/code-reviewer/SKILL.md
@skills/code-reviewer/references/README.md
@skills/code-reviewer/references/INSTALL.md
@skills/code-reviewer/assets/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add exact local validators and preserve the validated temp-project handoff</name>
  <files>package.json, scripts/validate-metadata.mjs, scripts/validate-docs.mjs, scripts/validate-phase-01.mjs, .planning/phases/01-package-foundation/01-VALIDATION-RUN.json</files>
  <read_first>package.json, README.md, skills/code-reviewer/SKILL.md, skills/code-reviewer/references/README.md, skills/code-reviewer/references/INSTALL.md, skills/code-reviewer/assets/README.md, .planning/phases/01-package-foundation/01-RESEARCH.md, .planning/phases/01-package-foundation/01-VALIDATION.md</read_first>
  <action>Update `package.json` to add these exact script entries: `"validate:metadata": "node scripts/validate-metadata.mjs"`, `"validate:docs": "node scripts/validate-docs.mjs"`, and `"validate:phase-01": "node scripts/validate-phase-01.mjs"`. Implement `scripts/validate-metadata.mjs` to fail non-zero unless `package.json` contains the exact Phase 1 manifest contract (`name: pi-code-reviewer`, `description: Pi-installable code review skill package.`, `keywords` including `pi-package`, `engines.node: >=20`, `files` including only `README.md` and `skills/`, and `pi.skills` equal to `["./skills"]`), required skill files exist, `skills/code-reviewer/SKILL.md` contains `name: code-reviewer`, `description:`, `/skill:code-reviewer`, and the exact marker `PHASE_1_PACKAGE_MARKER: pi-code-reviewer`. Implement `scripts/validate-docs.mjs` to fail non-zero unless `README.md` contains `pi install npm:pi-code-reviewer`, `npm install -g pi-code-reviewer`, `/skill:code-reviewer`, `./skills/code-reviewer/references/INSTALL.md`, and the note that plain npm installation is not guaranteed Pi registration by itself; and unless `skills/code-reviewer/references/INSTALL.md` contains `npm pack --silent`, `pi install -l "$UNPACK_DIR/package"`, `pi --no-session -p "/skill:code-reviewer"`, `PHASE_1_PACKAGE_MARKER: pi-code-reviewer`, and an explicit statement that marker output is required for a pass. Implement `scripts/validate-phase-01.mjs` so it runs `npm run validate:metadata` and `npm run validate:docs`, asserts required source files exist, runs `npm pack --silent`, inspects the generated tarball for `package/package.json`, `package/README.md`, `package/skills/code-reviewer/SKILL.md`, `package/skills/code-reviewer/references/README.md`, `package/skills/code-reviewer/references/INSTALL.md`, and `package/skills/code-reviewer/assets/README.md`, unpacks the tarball into a temp directory, creates a clean temp project, runs `pi install -l` against the unpacked `package/` directory, fails non-zero if `.pi/settings.json` does not record that unpacked package path, and writes `.planning/phases/01-package-foundation/01-VALIDATION-RUN.json` with exact keys `tarball`, `unpackDir`, `packageDir`, `testProject`, `packageName`, and `expectedMarker`. Do not delete the successful temp directories at the end of the script, because Task 3 reuses the recorded `testProject` for the smoke command.</action>
  <verify>npm run validate:metadata && npm run validate:docs && npm run validate:phase-01 && test -f .planning/phases/01-package-foundation/01-VALIDATION-RUN.json && grep -F '"testProject"' .planning/phases/01-package-foundation/01-VALIDATION-RUN.json</verify>
  <acceptance_criteria>
    - `package.json` contains the exact script strings `"validate:metadata": "node scripts/validate-metadata.mjs"`, `"validate:docs": "node scripts/validate-docs.mjs"`, and `"validate:phase-01": "node scripts/validate-phase-01.mjs"`.
    - `scripts/validate-metadata.mjs` verifies the exact marker `PHASE_1_PACKAGE_MARKER: pi-code-reviewer` in `skills/code-reviewer/SKILL.md`, and `scripts/validate-docs.mjs` verifies the exact marker requirement in `skills/code-reviewer/references/INSTALL.md`.
    - `npm run validate:phase-01` exits successfully and writes `.planning/phases/01-package-foundation/01-VALIDATION-RUN.json` with reusable `packageDir` and `testProject` paths from the validated install.
  </acceptance_criteria>
  <done>Phase 1 has exact local validators and a preserved validated temp-project handoff for downstream smoke verification.</done>
</task>

<task type="auto">
  <name>Task 2: Update the Phase 1 validation contract with exact marker-based smoke commands</name>
  <files>.planning/phases/01-package-foundation/01-VALIDATION.md</files>
  <read_first>.planning/phases/01-package-foundation/01-VALIDATION.md, package.json, scripts/validate-metadata.mjs, scripts/validate-docs.mjs, scripts/validate-phase-01.mjs, .planning/phases/01-package-foundation/01-VALIDATION-RUN.json, README.md, skills/code-reviewer/references/INSTALL.md</read_first>
  <action>Revise `.planning/phases/01-package-foundation/01-VALIDATION.md` so it references the exact shipped scripts from `package.json`, marks `nyquist_compliant: true` once the scripts exist, and updates the Wave 0 checklist to the concrete Phase 1 file set including `scripts/validate-metadata.mjs`, `scripts/validate-docs.mjs`, `scripts/validate-phase-01.mjs`, and `.planning/phases/01-package-foundation/01-VALIDATION-RUN.json`. Replace the generic/missing discovery entry with an explicit local smoke command sequence built from the recorded temp project: `npm run validate:phase-01`, then `TEST_PROJECT="$(node -p \"JSON.parse(require('node:fs').readFileSync('.planning/phases/01-package-foundation/01-VALIDATION-RUN.json','utf8')).testProject\")"`, then create a prompt file in that temp project containing `/skill:code-reviewer` on the first line and `Reply with the exact Phase 1 package marker and package name from the active skill instructions.` on the second line, and finally run `(cd "$TEST_PROJECT" && pi --no-session -p "$(cat "$TEST_PROJECT/.phase-01-smoke-prompt.txt")")`. State explicitly that success requires output containing both `PHASE_1_PACKAGE_MARKER: pi-code-reviewer` and `pi-code-reviewer`, and that a missing marker is a failure even if Pi resolves some `code-reviewer` skill. Keep the note that real npm-registry installation is deferred to the final release phase, but do not leave any `MISSING` placeholders for the local artifact/discovery flow.</action>
  <verify>grep -F 'nyquist_compliant: true' .planning/phases/01-package-foundation/01-VALIDATION.md && grep -F 'npm run validate:phase-01' .planning/phases/01-package-foundation/01-VALIDATION.md && grep -F '01-VALIDATION-RUN.json' .planning/phases/01-package-foundation/01-VALIDATION.md && grep -F 'PHASE_1_PACKAGE_MARKER: pi-code-reviewer' .planning/phases/01-package-foundation/01-VALIDATION.md</verify>
  <acceptance_criteria>
    - `.planning/phases/01-package-foundation/01-VALIDATION.md` contains `nyquist_compliant: true`.
    - `.planning/phases/01-package-foundation/01-VALIDATION.md` contains the exact command `npm run validate:phase-01` and references `.planning/phases/01-package-foundation/01-VALIDATION-RUN.json` as the smoke handoff artifact.
    - `.planning/phases/01-package-foundation/01-VALIDATION.md` states that success requires `PHASE_1_PACKAGE_MARKER: pi-code-reviewer` and no longer uses `MISSING` to describe the local artifact/discovery flow.
  </acceptance_criteria>
  <done>The validation strategy document is concrete, executable, and aligned with the preserved temp-project handoff plus the package-specific marker check.</done>
</task>

<task type="auto">
  <name>Task 3: Execute the recorded local smoke flow and capture the latest marker-based result</name>
  <files>.planning/phases/01-package-foundation/01-VALIDATION.md, .planning/phases/01-package-foundation/01-VALIDATION-RUN.json</files>
  <read_first>.planning/phases/01-package-foundation/01-VALIDATION.md, .planning/phases/01-package-foundation/01-VALIDATION-RUN.json, package.json, scripts/validate-phase-01.mjs, skills/code-reviewer/SKILL.md, skills/code-reviewer/references/INSTALL.md</read_first>
  <action>Run `npm run validate:phase-01` and then read `.planning/phases/01-package-foundation/01-VALIDATION-RUN.json` to recover the exact `testProject`, `packageDir`, and `expectedMarker` produced by the validated install. In that recorded temp project, create `.phase-01-smoke-prompt.txt` with exactly two lines: `/skill:code-reviewer` and `Reply with the exact Phase 1 package marker and package name from the active skill instructions.` Run the discovery smoke command from that same temp project by executing `(cd "$TEST_PROJECT" && pi --no-session -p "$(cat "$TEST_PROJECT/.phase-01-smoke-prompt.txt")")`; if the environment cannot run headless Pi, immediately run the documented interactive fallback from the same `testProject` instead. Append a `Latest local smoke result` section to `.planning/phases/01-package-foundation/01-VALIDATION.md` that records the exact command used, the `testProject` and `packageDir` values from `.planning/phases/01-package-foundation/01-VALIDATION-RUN.json`, and the observed result. Treat any missing-skill response, missing marker, or output that does not contain both `PHASE_1_PACKAGE_MARKER: pi-code-reviewer` and `pi-code-reviewer` as a plan failure. If the only blocker is local Pi authentication/provider setup, record that blocker text explicitly instead of inventing a pass.</action>
  <verify>npm run validate:phase-01 && test -f .planning/phases/01-package-foundation/01-VALIDATION-RUN.json && grep -F 'Latest local smoke result' .planning/phases/01-package-foundation/01-VALIDATION.md && grep -F '/skill:code-reviewer' .planning/phases/01-package-foundation/01-VALIDATION.md</verify>
  <acceptance_criteria>
    - `npm run validate:phase-01` passes before the smoke result is recorded.
    - `.planning/phases/01-package-foundation/01-VALIDATION.md` contains a `Latest local smoke result` section with the exact discovery command used and the `testProject` context recovered from `.planning/phases/01-package-foundation/01-VALIDATION-RUN.json`.
    - The recorded smoke result either contains both `PHASE_1_PACKAGE_MARKER: pi-code-reviewer` and `pi-code-reviewer`, or records an explicit Pi auth/provider blocker; it never claims success on a missing-skill or markerless response.
  </acceptance_criteria>
  <done>The packed-artifact install/discovery flow has been exercised against the preserved validated temp project, and the latest outcome is captured honestly with the package-specific marker check.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run validate:metadata && npm run validate:docs && npm run validate:phase-01`
- [ ] `test -f .planning/phases/01-package-foundation/01-VALIDATION-RUN.json && grep -F '"testProject"' .planning/phases/01-package-foundation/01-VALIDATION-RUN.json`
- [ ] `grep -F 'nyquist_compliant: true' .planning/phases/01-package-foundation/01-VALIDATION.md`
- [ ] `grep -F 'Latest local smoke result' .planning/phases/01-package-foundation/01-VALIDATION.md && grep -F '/skill:code-reviewer' .planning/phases/01-package-foundation/01-VALIDATION.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Validation proves the tarball includes every runtime file needed for Pi discovery
- Local `pi install -l` succeeds from the unpacked tarball, preserves the validated temp-project handoff, and records a marker-based `/skill:code-reviewer` smoke result honestly
  </success_criteria>

<output>
After completion, create `.planning/phases/01-package-foundation/01-03-SUMMARY.md`
</output>
