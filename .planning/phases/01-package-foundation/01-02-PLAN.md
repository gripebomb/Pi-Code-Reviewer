---
phase: 01-package-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - README.md
  - skills/code-reviewer/references/INSTALL.md
autonomous: true
requirements:
  - DIST-03
user_setup: []
must_haves:
  truths:
    - "Users can see both supported installation paths: `pi install npm:pi-code-reviewer` and `npm install -g pi-code-reviewer`."
    - "Users can see the canonical invocation command `/skill:code-reviewer` and a shared smoke-test path immediately after installation."
    - "The repo includes a detailed install/discovery smoke-test document that matches the Phase 1 packed-artifact verification flow."
    - "The detailed smoke-test document includes the unique marker `PHASE_1_PACKAGE_MARKER: pi-code-reviewer` so discovery verification can distinguish this package from any other installed `code-reviewer` skill."
  artifacts:
    - path: "README.md"
      provides: "User-facing installation and invocation documentation"
      contains: "pi install npm:pi-code-reviewer"
    - path: "skills/code-reviewer/references/INSTALL.md"
      provides: "Detailed Phase 1 smoke-test procedure"
      contains: "PHASE_1_PACKAGE_MARKER: pi-code-reviewer"
  key_links:
    - from: "README.md"
      to: "skills/code-reviewer/references/INSTALL.md"
      via: "README links to the detailed local smoke-test procedure"
    - from: "README.md"
      to: "/skill:code-reviewer"
      via: "README exposes the canonical post-install invocation command"
---

<objective>
Add end-user installation and invocation documentation, including the shared `/skill:code-reviewer` smoke test.

Purpose: Satisfy the Phase 1 documentation requirement so users can install the package through the supported paths and understand the honest current behavior.
Output: Root `README.md` plus `skills/code-reviewer/references/INSTALL.md` with the detailed smoke-test procedure.
</objective>

<execution_context>
@.pi/gsd/workflows/execute-plan.md
@.pi/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-package-foundation/01-CONTEXT.md
@.planning/phases/01-package-foundation/01-RESEARCH.md
@.planning/phases/01-package-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the root README with both install paths and the canonical invocation</name>
  <files>README.md</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-package-foundation/01-CONTEXT.md, .planning/phases/01-package-foundation/01-RESEARCH.md</read_first>
  <action>Create or overwrite `README.md` with user-first sections in this order: package overview, recommended Pi install path, standard npm install path, invocation, and smoke test. Include these exact commands verbatim: `pi install npm:pi-code-reviewer`, `npm install -g pi-code-reviewer`, and `/skill:code-reviewer`. Add an explicit note that Pi discovery is validated through `pi install`, while plain npm installation documents package availability but must not be presented as guaranteed Pi registration by itself. State clearly that Phase 1 only confirms install/discovery and that the full review workflow arrives in Phase 2. Add a markdown link to `./skills/code-reviewer/references/INSTALL.md` for the detailed local smoke-test checklist.</action>
  <verify>test -f README.md && grep -F 'pi install npm:pi-code-reviewer' README.md && grep -F 'npm install -g pi-code-reviewer' README.md && grep -F '/skill:code-reviewer' README.md && grep -F './skills/code-reviewer/references/INSTALL.md' README.md</verify>
  <acceptance_criteria>
    - `README.md` contains the exact strings `pi install npm:pi-code-reviewer`, `npm install -g pi-code-reviewer`, and `/skill:code-reviewer`.
    - `README.md` explicitly states that Phase 1 validates installation/discovery now and does not promise the full review workflow yet.
    - `README.md` links to `./skills/code-reviewer/references/INSTALL.md`.
  </acceptance_criteria>
  <done>The root README tells a new user exactly how to install and invoke the Phase 1 package without overpromising scope.</done>
</task>

<task type="auto">
  <name>Task 2: Add a detailed local smoke-test reference under the skill directory</name>
  <files>skills/code-reviewer/references/INSTALL.md</files>
  <read_first>.planning/phases/01-package-foundation/01-CONTEXT.md, .planning/phases/01-package-foundation/01-RESEARCH.md, .planning/phases/01-package-foundation/01-VALIDATION.md, README.md, skills/code-reviewer/SKILL.md</read_first>
  <action>Create `skills/code-reviewer/references/INSTALL.md` with a numbered procedure that matches the Phase 1 validation strategy: run `npm pack --silent`, unpack the generated `pi-code-reviewer-*.tgz` into a temp directory, create a clean temp project, run `pi install -l "$UNPACK_DIR/package"`, and then run `pi --no-session -p "/skill:code-reviewer"` as the basic entrypoint check with an interactive `pi` + `/skill:code-reviewer` fallback if the environment is not headless-safe. Add a second verification step that asks the active skill to return its exact Phase 1 package marker and package name, and document the expected success condition as output containing both `PHASE_1_PACKAGE_MARKER: pi-code-reviewer` and `pi-code-reviewer`. State explicitly that generic skill resolution without the marker is not a pass because another installed `code-reviewer` skill could false-pass the smoke test.</action>
  <verify>test -f skills/code-reviewer/references/INSTALL.md && grep -F 'npm pack --silent' skills/code-reviewer/references/INSTALL.md && grep -F 'pi install -l "$UNPACK_DIR/package"' skills/code-reviewer/references/INSTALL.md && grep -F 'pi --no-session -p "/skill:code-reviewer"' skills/code-reviewer/references/INSTALL.md && grep -F 'PHASE_1_PACKAGE_MARKER: pi-code-reviewer' skills/code-reviewer/references/INSTALL.md</verify>
  <acceptance_criteria>
    - `skills/code-reviewer/references/INSTALL.md` contains `npm pack --silent`, `pi install -l "$UNPACK_DIR/package"`, and `pi --no-session -p "/skill:code-reviewer"`.
    - The document states that success requires output containing the exact marker `PHASE_1_PACKAGE_MARKER: pi-code-reviewer`, not just any generic skill resolution.
    - The document includes an interactive Pi fallback for environments where `--no-session` smoke testing is not available.
  </acceptance_criteria>
  <done>The repository contains a detailed smoke-test checklist that matches the Phase 1 install/discovery contract and avoids false-passing on a different `code-reviewer` skill.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f README.md && grep -F 'pi install npm:pi-code-reviewer' README.md && grep -F 'npm install -g pi-code-reviewer' README.md && grep -F '/skill:code-reviewer' README.md`
- [ ] `test -f skills/code-reviewer/references/INSTALL.md && grep -F 'pi install -l "$UNPACK_DIR/package"' skills/code-reviewer/references/INSTALL.md && grep -F 'PHASE_1_PACKAGE_MARKER: pi-code-reviewer' skills/code-reviewer/references/INSTALL.md`
- [ ] `grep -F './skills/code-reviewer/references/INSTALL.md' README.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Documentation clearly supports both npm install and `pi install`
- The shared smoke test is centered on `/skill:code-reviewer`, matches the Phase 1 validation flow, and requires the package-specific Phase 1 marker for a real pass
  </success_criteria>

<output>
After completion, create `.planning/phases/01-package-foundation/01-02-SUMMARY.md`
</output>
