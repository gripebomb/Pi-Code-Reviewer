---
phase: 01-package-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - skills/code-reviewer/SKILL.md
  - skills/code-reviewer/references/README.md
  - skills/code-reviewer/assets/README.md
autonomous: true
requirements:
  - DIST-01
  - DIST-02
user_setup: []
must_haves:
  truths:
    - "The root package manifest targets the canonical unscoped npm package name `pi-code-reviewer` and declares `./skills` as the Pi skills directory."
    - "The shipped skill lives at `skills/code-reviewer/` and exposes a valid `SKILL.md` with `name: code-reviewer` and a non-empty description."
    - "The shipped skill exposes the exact unique marker `PHASE_1_PACKAGE_MARKER: pi-code-reviewer` so later smoke validation can distinguish this package from any other installed `code-reviewer` skill."
    - "Future-ready `references/` and `assets/` directories exist as packable markdown artifacts rather than empty folders."
  artifacts:
    - path: "package.json"
      provides: "npm + Pi package manifest for Phase 1 distribution"
      contains: '"name": "pi-code-reviewer"'
    - path: "skills/code-reviewer/SKILL.md"
      provides: "Phase 1 discoverable Pi skill entrypoint"
      contains: "PHASE_1_PACKAGE_MARKER: pi-code-reviewer"
    - path: "skills/code-reviewer/references/README.md"
      provides: "Packable placeholder for future skill reference docs"
      min_lines: 3
    - path: "skills/code-reviewer/assets/README.md"
      provides: "Packable placeholder for future skill assets"
      min_lines: 3
  key_links:
    - from: "package.json"
      to: "skills/"
      via: "`pi.skills` explicitly points Pi at the shipped skills directory"
    - from: "skills/code-reviewer/SKILL.md"
      to: "skills/code-reviewer/"
      via: "skill frontmatter name matches the directory name `code-reviewer` for Pi discovery"
---

<objective>
Create the publishable package scaffold, explicit Pi manifest, and future-ready skill directory layout.

Purpose: Establish the smallest honest Phase 1 package that Pi can discover later without reworking the repo structure in Phase 2.
Output: `package.json`, `skills/code-reviewer/SKILL.md`, and packable placeholder docs under `skills/code-reviewer/references/` and `skills/code-reviewer/assets/`.
</objective>

<execution_context>
@.pi/gsd/workflows/execute-plan.md
@.pi/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-package-foundation/01-CONTEXT.md
@.planning/phases/01-package-foundation/01-RESEARCH.md
@.planning/phases/01-package-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate on canonical npm package name availability and create the root package manifest</name>
  <files>package.json</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-package-foundation/01-CONTEXT.md, .planning/phases/01-package-foundation/01-RESEARCH.md</read_first>
  <action>Run `npm view pi-code-reviewer name >/tmp/pi-code-reviewer-name-check.log 2>&1` before writing any package files. If the command succeeds, stop the plan immediately and report that the canonical name `pi-code-reviewer` is unavailable; do not invent a fallback name. Only continue when the log shows an npm `E404`/not-found result for `pi-code-reviewer`; treat any other npm or network error as a blocker instead of proceeding. When the name is confirmed unavailable, create `package.json` at the repo root with exact Phase 1 values: `"name": "pi-code-reviewer"`, `"version": "0.1.0"`, `"description": "Pi-installable code review skill package."`, `"keywords": ["pi-package", "pi", "skill", "code-review"]`, `"license": "MIT"`, `"engines": { "node": ">=20" }`, `"files": ["README.md", "skills/"]`, and `"pi": { "skills": ["./skills"] }`. Do not add extension metadata, a `bin` entry, or runtime dependencies in this plan.</action>
  <verify>test -f package.json && grep -F '"name": "pi-code-reviewer"' package.json && grep -F '"pi-package"' package.json && grep -F '"skills": ["./skills"]' package.json && grep -F '"node": ">=20"' package.json</verify>
  <acceptance_criteria>
    - `package.json` contains the exact string `"name": "pi-code-reviewer"`.
    - `package.json` contains the exact string `"skills": ["./skills"]` under the `pi` manifest.
    - Execution halts with an explicit blocker instead of using any fallback package name if `npm view pi-code-reviewer name` succeeds, and it also halts on non-404 npm/network errors.
  </acceptance_criteria>
  <done>`package.json` exists with the canonical package metadata and no fallback naming was introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Create the discoverable skill entrypoint, unique Phase 1 marker, and packable placeholder directories</name>
  <files>skills/code-reviewer/SKILL.md, skills/code-reviewer/references/README.md, skills/code-reviewer/assets/README.md</files>
  <read_first>.planning/phases/01-package-foundation/01-CONTEXT.md, .planning/phases/01-package-foundation/01-RESEARCH.md, .planning/phases/01-package-foundation/01-VALIDATION.md</read_first>
  <action>Create `skills/code-reviewer/SKILL.md` with frontmatter `name: code-reviewer` and a non-empty `description:` that says the Phase 1 skill confirms package installation and `/skill:code-reviewer` resolution. In the body, keep the content intentionally minimal and honest: state that the skill exists to verify installation/discovery now, that the full review workflow lands in Phase 2, and include the exact marker line `PHASE_1_PACKAGE_MARKER: pi-code-reviewer` on its own line so downstream smoke validation can prove Pi resolved this package's skill rather than another `code-reviewer` skill. Also create `skills/code-reviewer/references/README.md` and `skills/code-reviewer/assets/README.md` as real markdown placeholders that explain those directories are reserved for future reference docs and shipped assets. Do not leave either directory empty and do not use `.gitkeep` files.</action>
  <verify>test -f skills/code-reviewer/SKILL.md && grep -F 'name: code-reviewer' skills/code-reviewer/SKILL.md && grep -F 'description:' skills/code-reviewer/SKILL.md && grep -F '/skill:code-reviewer' skills/code-reviewer/SKILL.md && grep -F 'PHASE_1_PACKAGE_MARKER: pi-code-reviewer' skills/code-reviewer/SKILL.md && test -f skills/code-reviewer/references/README.md && test -f skills/code-reviewer/assets/README.md</verify>
  <acceptance_criteria>
    - `skills/code-reviewer/SKILL.md` contains `name: code-reviewer` and a `description:` line.
    - `skills/code-reviewer/SKILL.md` contains the exact strings `/skill:code-reviewer` and `PHASE_1_PACKAGE_MARKER: pi-code-reviewer`.
    - `skills/code-reviewer/references/README.md` and `skills/code-reviewer/assets/README.md` both exist as markdown files, not empty directories.
  </acceptance_criteria>
  <done>The skill directory is discoverable, future-ready, and uniquely identifiable during Phase 1 smoke validation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f package.json && grep -F '"name": "pi-code-reviewer"' package.json && grep -F '"skills": ["./skills"]' package.json`
- [ ] `test -f skills/code-reviewer/SKILL.md && grep -F 'name: code-reviewer' skills/code-reviewer/SKILL.md && grep -F 'description:' skills/code-reviewer/SKILL.md && grep -F 'PHASE_1_PACKAGE_MARKER: pi-code-reviewer' skills/code-reviewer/SKILL.md`
- [ ] `test -f skills/code-reviewer/references/README.md && test -f skills/code-reviewer/assets/README.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The package scaffold uses the canonical package name `pi-code-reviewer` or stops cleanly if that name is unavailable
- Pi-facing skill resources exist under `skills/code-reviewer/` with packable placeholder subdirectories and a unique Phase 1 marker for later smoke verification
  </success_criteria>

<output>
After completion, create `.planning/phases/01-package-foundation/01-01-SUMMARY.md`
</output>
