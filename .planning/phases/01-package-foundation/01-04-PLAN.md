---
phase: 01-package-foundation
plan: "04"
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - .planning/phases/01-package-foundation/01-VALIDATION.md
autonomous: true
requirements:
  - DIST-01
  - DIST-02
user_setup: []
must_haves:
  truths:
    - "Phase 1 is not complete until the package version from `package.json` is published to the npm registry and can be installed from the registry without a local repo path."
    - "Registry verification proves both plain npm install and `pi install npm:pi-code-reviewer` resolve the published package version rather than the local unpacked artifact."
    - "Registry-based `/skill:code-reviewer` smoke uses the package-specific marker `PHASE_1_PACKAGE_MARKER: pi-code-reviewer`, so it cannot false-pass on another installed `code-reviewer` skill."
    - "Publish and registry-install evidence is recorded in the Phase 1 validation document with the exact version, commands, and observed results."
  artifacts:
    - path: ".planning/phases/01-package-foundation/01-VALIDATION.md"
      provides: "Recorded npm publish, registry install, and registry smoke evidence for Phase 1 sign-off"
      contains: "Registry publish result"
  key_links:
    - from: ".planning/phases/01-package-foundation/01-VALIDATION.md"
      to: "package.json"
      via: "validation doc records the exact published package version taken from the root manifest"
    - from: ".planning/phases/01-package-foundation/01-VALIDATION.md"
      to: "PHASE_1_PACKAGE_MARKER: pi-code-reviewer"
      via: "registry smoke evidence proves the published skill is this package's skill"
---

<objective>
Complete Phase 1 by publishing the canonical package and verifying real registry installs.

Purpose: Close the remaining `DIST-01` gap and confirm that registry-based npm and Pi installs behave like the previously validated local artifact.
Output: Updated `.planning/phases/01-package-foundation/01-VALIDATION.md` with recorded publish, registry npm install, registry `pi install`, and marker-based smoke evidence.
</objective>

<execution_context>
@.pi/gsd/workflows/execute-plan.md
@.pi/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-package-foundation/01-CONTEXT.md
@.planning/phases/01-package-foundation/01-RESEARCH.md
@.planning/phases/01-package-foundation/01-VALIDATION.md
@.planning/phases/01-package-foundation/01-VALIDATION-RUN.json
@package.json
@README.md
@skills/code-reviewer/SKILL.md
@skills/code-reviewer/references/INSTALL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish the exact Phase 1 package version to npm and record registry availability</name>
  <files>.planning/phases/01-package-foundation/01-VALIDATION.md</files>
  <read_first>package.json, .planning/phases/01-package-foundation/01-VALIDATION.md, .planning/phases/01-package-foundation/01-VALIDATION-RUN.json, skills/code-reviewer/references/INSTALL.md</read_first>
  <action>Read the exact version from `package.json` and store it in `VERSION`. Run `npm whoami` first; if it fails, stop and report an npm publish-auth blocker instead of pretending publish succeeded. Next run `npm view "pi-code-reviewer@$VERSION" version >/tmp/pi-code-reviewer-registry-version.log 2>&1`. If that command already returns `$VERSION`, stop and record that the version is already published so the release can be advanced deliberately rather than overwritten. Only when the version is absent with an npm 404/not-found response should you run `npm publish --access public` from the repo root. After publish, run `npm view "pi-code-reviewer@$VERSION" version` and `npm view pi-code-reviewer dist-tags --json` to confirm the version is live. Append a `Registry publish result` section to `.planning/phases/01-package-foundation/01-VALIDATION.md` that records the exact version, the exact publish command, and the confirmation output. Treat any auth error, non-404 preflight error, or publish failure as a blocker.</action>
  <verify>VERSION="$(node -p "require('./package.json').version")" && test -n "$VERSION" && npm view "pi-code-reviewer@$VERSION" version | grep -Fx "$VERSION"</verify>
  <acceptance_criteria>
    - The exact `package.json` version is available from the npm registry as `pi-code-reviewer@$VERSION` after the task completes.
    - `.planning/phases/01-package-foundation/01-VALIDATION.md` contains a `Registry publish result` section with the exact publish command and published version.
    - Execution stops with an explicit blocker instead of assuming success when npm publish auth is missing or the target version is already published.
  </acceptance_criteria>
  <done>The canonical Phase 1 package version is published to npm or an explicit publish blocker is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Verify registry npm install, registry `pi install`, and marker-based discovery against the published package</name>
  <files>.planning/phases/01-package-foundation/01-VALIDATION.md</files>
  <read_first>.planning/phases/01-package-foundation/01-VALIDATION.md, package.json, skills/code-reviewer/SKILL.md, skills/code-reviewer/references/INSTALL.md</read_first>
  <action>Using the exact published `VERSION` from Task 1, create a clean temp directory `REGISTRY_NPM_TMP="$(mktemp -d)"` and run `npm install --prefix "$REGISTRY_NPM_TMP" "pi-code-reviewer@$VERSION"`. Verify the installed package contains `skills/code-reviewer/SKILL.md`, `skills/code-reviewer/references/INSTALL.md`, and `skills/code-reviewer/assets/README.md`, and verify the installed `SKILL.md` contains the exact marker `PHASE_1_PACKAGE_MARKER: pi-code-reviewer`. Then create a clean temp Pi project `REGISTRY_PI_PROJECT="$(mktemp -d)"` and run `(cd "$REGISTRY_PI_PROJECT" && pi install -l "npm:pi-code-reviewer@$VERSION")` so the registry install is recorded in that temp project's `.pi/settings.json` rather than in global Pi settings. In that same temp project, verify `.pi/settings.json` exists and records the exact source string `npm:pi-code-reviewer@$VERSION`. Next create `.phase-01-registry-smoke-prompt.txt` with exactly two lines: `/skill:code-reviewer` and `Reply with the exact Phase 1 package marker and package name from the active skill instructions.` Run `(cd "$REGISTRY_PI_PROJECT" && pi --no-session -p "$(cat "$REGISTRY_PI_PROJECT/.phase-01-registry-smoke-prompt.txt")")`; if the environment cannot run headless Pi, immediately run the documented interactive fallback from the same temp project instead. Treat any response that does not contain both `PHASE_1_PACKAGE_MARKER: pi-code-reviewer` and `pi-code-reviewer` as a failure, because that does not prove the published package's skill was resolved. Append a `Latest registry install result` section to `.planning/phases/01-package-foundation/01-VALIDATION.md` that records the exact npm install command, the exact project-local `pi install -l` command, the `REGISTRY_PI_PROJECT` context, whether `.pi/settings.json` recorded `npm:pi-code-reviewer@$VERSION`, and the observed marker-based smoke result. If the only blocker is Pi auth/provider setup, record that blocker explicitly instead of inventing a pass.</action>
  <verify>VERSION="$(node -p "require('./package.json').version")" && REGISTRY_NPM_TMP="$(mktemp -d)" && npm install --prefix "$REGISTRY_NPM_TMP" "pi-code-reviewer@$VERSION" >/tmp/pi-code-reviewer-registry-install.log 2>&1 && test -f "$REGISTRY_NPM_TMP/node_modules/pi-code-reviewer/skills/code-reviewer/SKILL.md" && grep -F 'PHASE_1_PACKAGE_MARKER: pi-code-reviewer' "$REGISTRY_NPM_TMP/node_modules/pi-code-reviewer/skills/code-reviewer/SKILL.md" && REGISTRY_PI_PROJECT="$(mktemp -d)" && (cd "$REGISTRY_PI_PROJECT" && pi install -l "npm:pi-code-reviewer@$VERSION") >/tmp/pi-code-reviewer-registry-pi-install.log 2>&1 && test -f "$REGISTRY_PI_PROJECT/.pi/settings.json" && grep -F "npm:pi-code-reviewer@$VERSION" "$REGISTRY_PI_PROJECT/.pi/settings.json" && grep -F 'Latest registry install result' .planning/phases/01-package-foundation/01-VALIDATION.md</verify>
  <acceptance_criteria>
    - `npm install --prefix "$REGISTRY_NPM_TMP" "pi-code-reviewer@$VERSION"` succeeds and the installed package contains the Phase 1 skill plus marker-bearing docs/assets.
    - `(cd "$REGISTRY_PI_PROJECT" && pi install -l "npm:pi-code-reviewer@$VERSION")` succeeds, `$REGISTRY_PI_PROJECT/.pi/settings.json` exists, and it records the exact source string `npm:pi-code-reviewer@$VERSION`.
    - `.planning/phases/01-package-foundation/01-VALIDATION.md` contains a `Latest registry install result` section with the exact npm install command, exact `pi install -l` command, temp-project context, and either a marker-based smoke success or an explicit Pi auth/provider blocker.
  </acceptance_criteria>
  <done>Phase 1 has real registry-install evidence for both npm and Pi flows, or an explicit blocker is captured honestly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `VERSION="$(node -p "require('./package.json').version")" && npm view "pi-code-reviewer@$VERSION" version | grep -Fx "$VERSION"`
- [ ] `grep -F 'Registry publish result' .planning/phases/01-package-foundation/01-VALIDATION.md`
- [ ] `grep -F 'Latest registry install result' .planning/phases/01-package-foundation/01-VALIDATION.md && grep -F 'PHASE_1_PACKAGE_MARKER: pi-code-reviewer' .planning/phases/01-package-foundation/01-VALIDATION.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The package version from `package.json` is published to the npm registry
- Clean registry-based npm and Pi installs are exercised and the package-specific marker proves `/skill:code-reviewer` resolved this published package
  </success_criteria>

<output>
After completion, create `.planning/phases/01-package-foundation/01-04-SUMMARY.md`
</output>
