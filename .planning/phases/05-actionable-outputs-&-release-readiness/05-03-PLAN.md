---
phase: 05-actionable-outputs-&-release-readiness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fixtures/fixture-a/.planning/REVIEW.md
  - test/fixtures/fixture-a/.planning/REVIEW-TODO.md
  - test/fixtures/fixture-b/.planning/REVIEW.md
  - test/fixtures/fixture-b/.planning/REVIEW-TODO.md
  - test/fixtures/fixture-c/.planning/REVIEW.md
  - test/fixtures/fixture-c/.planning/REVIEW-TODO.md
  - scripts/validate-phase-05.mjs
  - README.md
  - skills/code-reviewer/references/INSTALL.md
autonomous: false
requirements:
  - OUTP-01
  - OUTP-02
  - OUTP-03
  - DIST-03
  - D-07
  - D-11
must_haves:
  truths:
    - "Fixture repos have generated REVIEW.md and REVIEW-TODO.md with prioritized format"
    - "Phase 5 validation script passes against fixture outputs"
    - "README.md documents the prioritized output and how to read it"
    - "INSTALL.md includes smoke-test instructions that verify prioritized output"
  artifacts:
    - path: "test/fixtures/fixture-a/.planning/REVIEW.md"
      provides: "Example prioritized review for simple repo"
      contains: "## Summary|## Prioritized Issue Table"
    - path: "test/fixtures/fixture-b/.planning/REVIEW.md"
      provides: "Example prioritized review for security-focused repo"
      contains: "## Summary|## Prioritized Issue Table"
    - path: "test/fixtures/fixture-c/.planning/REVIEW.md"
      provides: "Example prioritized review for edge-case repo"
      contains: "## Summary|## Prioritized Issue Table"
    - path: "scripts/validate-phase-05.mjs"
      provides: "Release-readiness validation script"
      exports: ["validateReleaseReadiness"]
    - path: "README.md"
      provides: "User-facing docs with output format explanation"
      contains: "prioritized|Summary|issue table"
  key_links:
    - from: "scripts/validate-phase-05.mjs"
      to: "test/validate-phase-05.mjs"
      via: "Calls Phase 5 output validator"
      pattern: "validate-phase-05"
    - from: "README.md"
      to: "skills/code-reviewer/references/INSTALL.md"
      via: "README references install docs"
      pattern: "INSTALL.md"
---

<objective>
Harden fixture coverage with prioritized output examples, create release-readiness validation, and polish documentation. This plan ensures the package has realistic examples, automated release checks, and clear user documentation.

Purpose: Make the package production-ready with verified examples and clear install/use instructions.
Output: Fixture repos with complete prioritized output, release validation script, and updated docs.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-actionable-outputs-&-release-readiness/05-CONTEXT.md

# Existing fixture and validation infrastructure
@test/validate-analysis.js
@test/fixtures/fixture-a/
@test/fixtures/fixture-b/
@test/fixtures/fixture-c/
@skills/code-reviewer/references/output-contract.md
@skills/code-reviewer/references/INSTALL.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate prioritized REVIEW.md and REVIEW-TODO.md for all fixture repos</name>
  <files>
    test/fixtures/fixture-a/.planning/REVIEW.md
    test/fixtures/fixture-a/.planning/REVIEW-TODO.md
    test/fixtures/fixture-b/.planning/REVIEW.md
    test/fixtures/fixture-b/.planning/REVIEW-TODO.md
    test/fixtures/fixture-c/.planning/REVIEW.md
    test/fixtures/fixture-c/.planning/REVIEW-TODO.md
  </files>
  <read_first>
    - skills/code-reviewer/references/output-contract.md
    - skills/code-reviewer/assets/review-template.md
    - skills/code-reviewer/assets/todo-template.md
    - test/fixtures/fixture-a/package.json
    - test/fixtures/fixture-b/package.json
    - test/fixtures/fixture-c/package.json
  </read_first>
  <action>
For each fixture repository (fixture-a, fixture-b, fixture-c), create `.planning/REVIEW.md` and `.planning/REVIEW-TODO.md` that demonstrate the complete prioritized output format.

**Fixture A** (simple repo with code quality + docs issues):
- Create `test/fixtures/fixture-a/.planning/REVIEW.md` with:
  - Header: `# Code Review: fixture-a`, date, scope: Whole repository
  - Summary table with realistic counts (e.g., Code Quality: 1 High, 2 Medium, 1 Low)
  - All 5 category sections with summary assessments and findings
  - Prioritized Issue Table with all findings sorted correctly
- Create `test/fixtures/fixture-a/.planning/REVIEW-TODO.md` with:
  - Header with date and repository name
  - All 5 category sections
  - Checklist items with severity badges, file references, and impact notes
  - Sorted by severity within each category

**Fixture B** (security-focused repo):
- Create `test/fixtures/fixture-b/.planning/REVIEW.md` with security-focused findings
- Include at least 1 High severity security finding
- Include cross-category references (e.g., security issue also noted in Test Coverage)
- Same structure as Fixture A

**Fixture C** (edge case — minimal repo):
- Create `test/fixtures/fixture-c/.planning/REVIEW.md` with mostly empty categories
- Demonstrate uncertainty handling: "No test files were found in the repository."
- Summary table should show zeros for most categories
- Prioritized Issue Table should still be present even with few findings

For all fixtures:
- Ensure file references match actual files in the fixture repo
- Ensure Summary table counts match actual findings
- Ensure Prioritized Issue Table includes every finding
- Ensure sort order is correct
- Ensure TODO items match findings

Create `.planning/` directories if they don't exist.
  </action>
  <verify>
    <automated>for f in fixture-a fixture-b fixture-c; do test -f "test/fixtures/$f/.planning/REVIEW.md" && echo "$f REVIEW.md exists" || echo "$f REVIEW.md MISSING"; done</automated>
    <automated>for f in fixture-a fixture-b fixture-c; do test -f "test/fixtures/$f/.planning/REVIEW-TODO.md" && echo "$f TODO.md exists" || echo "$f TODO.md MISSING"; done</automated>
    <automated>grep -c "## Summary" test/fixtures/fixture-a/.planning/REVIEW.md</automated>
    <automated>grep -c "## Prioritized Issue Table" test/fixtures/fixture-a/.planning/REVIEW.md</automated>
    <automated>grep -c "\*\*\[High\]\*\*" test/fixtures/fixture-a/.planning/REVIEW-TODO.md</automated>
  </verify>
  <done>
    - All 3 fixture repos have REVIEW.md with Summary table and Prioritized Issue Table
    - All 3 fixture repos have REVIEW-TODO.md with severity badges and impact notes
    - Fixture A has realistic multi-severity findings
    - Fixture B has security-focused findings with cross-category references
    - Fixture C demonstrates empty-category handling with uncertainty statements
  </done>
</task>

<task type="auto">
  <name>Task 2: Create release-readiness validation script</name>
  <files>scripts/validate-phase-05.mjs</files>
  <read_first>
    - scripts/validate-phase-01.mjs
    - scripts/validate-phase-02.mjs
    - scripts/validate-phase-03.mjs
    - test/validate-phase-05.mjs (from Plan 05-02)
    - package.json
  </read_first>
  <action>
Create `scripts/validate-phase-05.mjs` that performs release-readiness checks for the entire package.

The script must:

1. **Validate package metadata**:
   - Read package.json
   - Verify `name` is "pi-code-reviewer"
   - Verify `version` is a valid semver string
   - Verify `keywords` includes "pi-package", "pi", "skill", "code-review"
   - Verify `pi.skills` points to "./skills"
   - Verify `files` includes "README.md" and "skills/"

2. **Validate skill structure**:
   - Verify `skills/code-reviewer/SKILL.md` exists
   - Verify `skills/code-reviewer/references/output-contract.md` exists
   - Verify `skills/code-reviewer/references/severity-guidelines.md` exists
   - Verify `skills/code-reviewer/references/review-rubric.md` exists
   - Verify `skills/code-reviewer/assets/review-template.md` exists
   - Verify `skills/code-reviewer/assets/todo-template.md` exists

3. **Validate fixture outputs** (call test/validate-phase-05.mjs):
   - Run the Phase 5 output validator against all 3 fixture repos
   - Verify REVIEW.md and REVIEW-TODO.md structure
   - Verify Summary table counts, Prioritized table completeness, and TODO structure

4. **Validate CLI support** (per D-09):
   - Verify package.json has a `bin` entry (or warn if missing — this is a release readiness check, not a hard failure if bin is planned for later)
   - If `bin` exists, verify the referenced file exists

5. **Validate documentation**:
   - Verify README.md exists and is non-empty (> 100 bytes)
   - Verify skills/code-reviewer/references/INSTALL.md exists

6. **Output format**:
   ```javascript
   console.log('Phase 5 Release Readiness Validation');
   console.log('=====================================');
   // ... per-check output
   
   if (allPassed) {
     console.log('\n✓ All release-readiness checks passed');
     process.exit(0);
   } else {
     console.log(`\n✗ ${failureCount} release-readiness check(s) failed`);
     process.exit(1);
   }
   ```

Use ES module syntax (`.mjs`). Follow patterns from existing validate-phase-*.mjs scripts.
  </action>
  <verify>
    <automated>node --check scripts/validate-phase-05.mjs 2>&1 || echo "Syntax check"</automated>
    <automated>grep -c "validate-phase-05.mjs" scripts/validate-phase-05.mjs | head -1</automated>
    <automated>grep -c "pi-code-reviewer" scripts/validate-phase-05.mjs</automated>
    <automated>grep -c "All release-readiness checks passed" scripts/validate-phase-05.mjs</automated>
  </verify>
  <done>
    - validate-phase-05.mjs exists with valid syntax
    - Script validates package metadata, skill structure, fixture outputs, CLI support, and docs
    - Script calls test/validate-phase-05.mjs for output structure validation
    - Script exits 0 on success, 1 on failure
  </done>
</task>

<task type="auto">
  <name>Task 3: Update README.md and INSTALL.md with output format documentation</name>
  <files>
    README.md
    skills/code-reviewer/references/INSTALL.md
  </files>
  <read_first>
    - README.md
    - skills/code-reviewer/references/INSTALL.md
    - skills/code-reviewer/references/output-contract.md
  </read_first>
  <action>
Update `README.md` to document the prioritized output format.

Add a new section "Output Format" after the existing usage section (or near the end if no clear usage section exists):

```markdown
## Output Format

After running the code reviewer, you'll find two files in `.planning/`:

### REVIEW.md

A comprehensive review report with:
- **Summary table** — counts of findings per category and severity at a glance
- **Category sections** — detailed findings grouped by Code Quality, Refactoring, Documentation, Security, and Test Coverage
- **Prioritized Issue Table** — all findings sorted by severity (High → Medium → Low) so you can decide what to fix first

### REVIEW-TODO.md

An actionable checklist derived from the findings:
- Grouped by category
- Sorted by severity within each category
- Each item includes a severity badge, file reference, and impact note
- Check off items as you fix them, or hand the list to another agent

### Severity Levels

- **High** — Immediate risk or broken behavior. Fix first.
- **Medium** — Fragility or significant improvement opportunity. Fix next.
- **Low** — Polish or incremental improvement. Fix when convenient.
```

Update `skills/code-reviewer/references/INSTALL.md` to include smoke-test instructions that verify prioritized output:

Add after existing installation steps:

```markdown
## Smoke Test

After installation, verify the skill produces prioritized output:

1. Run the skill against a test repository:
   ```
   /skill:code-reviewer
   ```

2. Check that `.planning/REVIEW.md` contains:
   - A `## Summary` table with category and severity counts
   - Five category sections with findings
   - A `## Prioritized Issue Table` sorted by severity

3. Check that `.planning/REVIEW-TODO.md` contains:
   - Categorized checklist items
   - Severity badges (`**[High]**`, `**[Medium]**`, `**[Low]**`)
   - File references and impact notes

4. Verify the Summary table counts match the actual findings in the category sections.
```

Ensure existing content is preserved. Additions should be clearly marked new sections.
  </action>
  <verify>
    <automated>grep -c "## Output Format" README.md</automated>
    <automated>grep -c "REVIEW.md" README.md</automated>
    <automated>grep -c "REVIEW-TODO.md" README.md</automated>
    <automated>grep -c "## Smoke Test" skills/code-reviewer/references/INSTALL.md</automated>
    <automated>grep -c "Prioritized Issue Table" skills/code-reviewer/references/INSTALL.md</automated>
  </verify>
  <done>
    - README.md has an "Output Format" section documenting REVIEW.md and REVIEW-TODO.md
    - README.md explains Summary table, category sections, and Prioritized Issue Table
    - README.md documents severity levels (High/Medium/Low)
    - INSTALL.md has a "Smoke Test" section with prioritized output verification steps
    - Existing content in both files is preserved
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture output → Validation script | Fixture examples must match actual skill output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | Fixture REVIEW.md | accept | Fixtures contain synthetic issues for testing, no real secrets |
| T-05-05 | Tampering | validate-phase-05.mjs | mitigate | Script validates package integrity and output structure |
</threat_model>

<verification>
1. All 3 fixture repos have complete REVIEW.md and REVIEW-TODO.md
2. Fixture outputs demonstrate Summary table, Prioritized table, and TODO format
3. Release-readiness validation script exists and validates package metadata, skill structure, fixtures, CLI, and docs
4. README.md documents the output format
5. INSTALL.md includes smoke-test instructions for prioritized output
</verification>

<success_criteria>
- Fixture repos have realistic prioritized output examples for testing
- Release-readiness validation script checks all critical package health indicators
- README.md explains the output format to users
- INSTALL.md includes smoke-test steps that verify prioritized output
</success_criteria>

<output>
After completion, create `.planning/phases/05-actionable-outputs-&-release-readiness/05-03-SUMMARY.md`
</output>
