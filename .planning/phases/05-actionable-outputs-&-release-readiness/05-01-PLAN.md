---
phase: 05-actionable-outputs-&-release-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/code-reviewer/references/output-contract.md
  - skills/code-reviewer/SKILL.md
  - skills/code-reviewer/assets/review-template.md
  - skills/code-reviewer/assets/todo-template.md
autonomous: true
requirements:
  - RPRT-03
  - OUTP-01
  - OUTP-02
must_haves:
  truths:
    - "Output contract defines a prioritized issue table format with category × severity grouping"
    - "Output contract defines a flat Summary section showing total findings per category × severity"
    - "SKILL.md Phase 5 references the prioritized output format and includes generation instructions"
    - "Review and TODO templates include examples of prioritized output"
  artifacts:
    - path: "skills/code-reviewer/references/output-contract.md"
      provides: "Prioritized issue table format and Summary section specification"
      contains: "Summary|prioritized|severity|category"
    - path: "skills/code-reviewer/SKILL.md"
      provides: "Phase 5 write-report instructions with prioritized output"
      contains: "prioritized|Summary|issue table"
    - path: "skills/code-reviewer/assets/review-template.md"
      provides: "Example prioritized report output"
      contains: "Summary|### High|### Medium|### Low"
    - path: "skills/code-reviewer/assets/todo-template.md"
      provides: "Example prioritized TODO checklist"
      contains: "**[High]**|**[Medium]**|**[Low]**"
  key_links:
    - from: "skills/code-reviewer/SKILL.md"
      to: "references/output-contract.md"
      via: "Phase 5 references output-contract for formatting rules"
      pattern: "output-contract.md"
    - from: "skills/code-reviewer/references/output-contract.md"
      to: "REVIEW.md"
      via: "Contract defines REVIEW.md structure"
      pattern: "REVIEW.md"
---

<objective>
Define the prioritized output format for review artifacts. This plan updates the output contract, skill workflow, and templates to specify how findings are organized into a prioritized issue table with summary statistics, per D-01, D-02, and D-03.

Purpose: Ensure every review produces consistently structured, prioritized output that helps users decide what to fix first.
Output: Updated output-contract.md, SKILL.md Phase 5, and template files with prioritized format examples.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-actionable-outputs-&-release-readiness/05-CONTEXT.md

# Existing skill files
@skills/code-reviewer/SKILL.md
@skills/code-reviewer/references/output-contract.md
@skills/code-reviewer/references/severity-guidelines.md
@skills/code-reviewer/assets/review-template.md
@skills/code-reviewer/assets/todo-template.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add prioritized issue table and Summary section to output contract</name>
  <files>skills/code-reviewer/references/output-contract.md</files>
  <read_first>
    - skills/code-reviewer/references/output-contract.md
    - skills/code-reviewer/references/severity-guidelines.md
  </read_first>
  <action>
Update `skills/code-reviewer/references/output-contract.md` to add two new sections after the existing "Category Sections" content and before "REVIEW-TODO.md Structure":

1. **Add "Summary Section"** (new section, placed before Category Sections or after the header):
   Insert after the Header section and before Category Sections:
   ```markdown
   ## Summary

   Immediately after the header, include a summary table showing total findings per category and severity.

   ```markdown
   ## Summary

   | Category | High | Medium | Low | Total |
   |----------|------|--------|-----|-------|
   | Code Quality | 1 | 2 | 1 | 4 |
   | Refactoring | 0 | 1 | 2 | 3 |
   | Documentation | 0 | 0 | 1 | 1 |
   | Security | 1 | 0 | 0 | 1 |
   | Test Coverage | 0 | 1 | 1 | 2 |
   | **Total** | **2** | **4** | **5** | **11** |
   ```

   Rules:
   - Include this section in every review, even if all counts are zero.
   - Use bold for the Total row.
   - Categories must appear in the standard order: Code Quality, Refactoring, Documentation, Security, Test Coverage.
   - If a category has no findings, show `0` in each severity column.
   ```

2. **Add "Prioritized Issue Table"** (new section after Category Sections):
   ```markdown
   ## Prioritized Issue Table

   After the five category sections, include a flat prioritized table that lists all findings sorted by severity first (High → Medium → Low), then by category within each severity level.

   ```markdown
   ## Prioritized Issue Table

   | Priority | Category | Finding | Location |
   |----------|----------|---------|----------|
   | **High** | Security | Hardcoded API key in config | `src/config.ts:42` |
   | **High** | Code Quality | Unhandled promise rejection | `src/app.ts:15` |
   | **Medium** | Code Quality | Mixed import styles | `src/utils/*.ts` |
   | **Medium** | Refactoring | Duplicated validation logic | `src/auth.ts:30`, `src/api.ts:45` |
   | **Medium** | Test Coverage | Missing error path tests | `tests/app.test.ts` |
   | **Low** | Code Quality | Debug console.log left in code | `src/server.ts:87` |
   | **Low** | Refactoring | Unused helper function | `src/helpers.ts:12` |
   | **Low** | Documentation | Missing usage examples | `README.md` |
   | **Low** | Test Coverage | Brittle assertion pattern | `tests/utils.test.ts:8` |
   | **Low** | Test Coverage | Missing edge case coverage | `tests/app.test.ts` |
   ```

   Rules:
   - Every finding from the category sections must appear in this table.
   - Sort order: High severity first (alphabetical by category within High), then Medium (alphabetical by category), then Low (alphabetical by category).
   - The "Finding" column should be a brief 3-8 word summary of the issue.
   - The "Location" column should include the backtick-wrapped file/path reference from the finding.
   - If a finding spans multiple locations, list all separated by commas.
   - This table is what users scan first to decide what to fix next.
   ```

3. **Update Category Sections rules** to reference the Summary:
   Add to the existing Category Sections rules list:
   - "The Summary section must appear immediately after the header, before any category section."
   - "Category sections must maintain the standard order: Code Quality, Refactoring, Documentation, Security, Test Coverage."

Ensure the existing content (Header, Path Reference Requirements, Category Sections, REVIEW-TODO.md Structure) is preserved. Only add the new sections and rules.
  </action>
  <verify>
    <automated>grep -c "## Summary" skills/code-reviewer/references/output-contract.md</automated>
    <automated>grep -c "## Prioritized Issue Table" skills/code-reviewer/references/output-contract.md</automated>
    <automated>grep -c "| Category | High | Medium | Low | Total |" skills/code-reviewer/references/output-contract.md</automated>
    <automated>grep -c "| Priority | Category | Finding | Location |" skills/code-reviewer/references/output-contract.md</automated>
  </verify>
  <done>
    - output-contract.md contains a "Summary" section with a category × severity table format
    - output-contract.md contains a "Prioritized Issue Table" section with flat sorted table format
    - Existing content (Header, Path References, Category Sections, TODO Structure) is preserved
  </done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md Phase 5 with prioritized output instructions</name>
  <files>skills/code-reviewer/SKILL.md</files>
  <read_first>
    - skills/code-reviewer/SKILL.md
    - skills/code-reviewer/references/output-contract.md (after Task 1 updates)
  </read_first>
  <action>
Update `skills/code-reviewer/SKILL.md` Phase 5 "Write Report" section to include prioritized output generation instructions.

Replace the current Phase 5 content:
```markdown
## Phase 5: Write Report

◆ Writing review report...

Before writing, read references/output-contract.md for formatting rules.
Before writing, read assets/review-template.md for report shape guidance.
Before writing, read assets/todo-template.md for TODO checklist shape guidance.
Write `.planning/REVIEW.md` using the required five-category structure.
Write `.planning/REVIEW-TODO.md` as the action checklist derived from the findings.
If `.planning/REVIEW.md` already existed before this run, add a note at the top stating: "This review replaces a previous review." In other words, the previous review was replaced rather than preserved.
```

With:
```markdown
## Phase 5: Write Report

◆ Writing review report...

Before writing, read references/output-contract.md for formatting rules.
Before writing, read assets/review-template.md for report shape guidance.
Before writing, read assets/todo-template.md for TODO checklist shape guidance.

### Report Structure

Write `.planning/REVIEW.md` in this order:

1. **Header** — repository name, date, scope (per output-contract.md)
2. **Summary** — category × severity count table with totals
3. **Category Sections** — Code Quality, Refactoring, Documentation, Security, Test Coverage (each with summary assessment + findings grouped by severity)
4. **Prioritized Issue Table** — flat table of all findings sorted by severity (High → Medium → Low), then by category within each severity

Write `.planning/REVIEW-TODO.md` as the action checklist derived from the findings, grouped by category with severity badges.

If `.planning/REVIEW.md` already existed before this run, add a note at the top stating: "This review replaces a previous review." In other words, the previous review was replaced rather than preserved.

### Quality Checks

Before finishing, verify:
- [ ] Summary table counts match actual findings in category sections
- [ ] Prioritized Issue Table includes every finding from category sections
- [ ] Sort order is correct: High severity first, then Medium, then Low; alphabetical by category within each severity
- [ ] Every finding in the table has a brief 3-8 word summary and a location reference
- [ ] TODO checklist items match findings and are sorted by severity within each category
```

Ensure the rest of SKILL.md (Phases 1-4, Rules, Reference Docs) is unchanged.
  </action>
  <verify>
    <automated>grep -c "## Summary" skills/code-reviewer/SKILL.md</automated>
    <automated>grep -c "Prioritized Issue Table" skills/code-reviewer/SKILL.md</automated>
    <automated>grep -c "category × severity count table" skills/code-reviewer/SKILL.md</automated>
    <automated>grep -c "flat table of all findings sorted by severity" skills/code-reviewer/SKILL.md</automated>
  </verify>
  <done>
    - SKILL.md Phase 5 includes instructions to generate Summary table
    - SKILL.md Phase 5 includes instructions to generate Prioritized Issue Table
    - SKILL.md Phase 5 includes quality checks for count consistency and sort order
    - Phases 1-4 and Rules sections are unchanged
  </done>
</task>

<task type="auto">
  <name>Task 3: Update review and TODO templates with prioritized examples</name>
  <files>
    skills/code-reviewer/assets/review-template.md
    skills/code-reviewer/assets/todo-template.md
  </files>
  <read_first>
    - skills/code-reviewer/assets/review-template.md
    - skills/code-reviewer/assets/todo-template.md
    - skills/code-reviewer/references/output-contract.md (after Task 1 updates)
  </read_first>
  <action>
Update `skills/code-reviewer/assets/review-template.md` to include a complete example of the prioritized output format.

Add after the existing "Example Section" content and before any closing content:

```markdown
## Complete Prioritized Report Example

```markdown
# Code Review: my-project

**Date:** 2026-04-20
**Scope:** Whole repository

## Summary

| Category | High | Medium | Low | Total |
|----------|------|--------|-----|-------|
| Code Quality | 1 | 2 | 1 | 4 |
| Refactoring | 0 | 1 | 2 | 3 |
| Documentation | 0 | 0 | 1 | 1 |
| Security | 1 | 0 | 0 | 1 |
| Test Coverage | 0 | 1 | 1 | 2 |
| **Total** | **2** | **4** | **5** | **11** |

## Code Quality

The codebase is generally readable, but error handling is inconsistent in a few important paths.

### High

- **Unhandled promise rejection in `parseConfig()`** (`src/config.ts:42`)
  The error path isn't handled — if the config file is malformed, the promise rejection
  will crash the process. Wrap in try/catch or add a `.catch()` handler.

### Medium

- **Mixed import styles** (`src/utils/*.ts`)
  Some files use `import type { ... }` and others use `import { ... }` for type-only
  imports. Standardizing on `import type` would make intent clearer.

### Low

- **Console.log in production code** (`src/server.ts:87`)
  A debug `console.log` statement appears to be left in the server startup path.
  Consider removing it or gating it behind a debug flag.

## Refactoring

## Documentation

## Security

### High

- **Hardcoded API key in configuration** (`src/config.ts:42`)
  The API key is stored directly in source code. Move to environment variables or a
  secrets manager.

## Test Coverage

## Prioritized Issue Table

| Priority | Category | Finding | Location |
|----------|----------|---------|----------|
| **High** | Code Quality | Unhandled promise rejection | `src/config.ts:42` |
| **High** | Security | Hardcoded API key in config | `src/config.ts:42` |
| **Medium** | Code Quality | Mixed import styles | `src/utils/*.ts` |
| **Medium** | Refactoring | Duplicated validation logic | `src/auth.ts:30`, `src/api.ts:45` |
| **Medium** | Test Coverage | Missing error path tests | `tests/app.test.ts` |
| **Low** | Code Quality | Debug console.log left in code | `src/server.ts:87` |
| **Low** | Refactoring | Unused helper function | `src/helpers.ts:12` |
| **Low** | Documentation | Missing usage examples | `README.md` |
| **Low** | Test Coverage | Brittle assertion pattern | `tests/utils.test.ts:8` |
| **Low** | Test Coverage | Missing edge case coverage | `tests/app.test.ts` |
```
```

Update `skills/code-reviewer/assets/todo-template.md` to include a complete example with severity badges and file references:

Add after the existing "Example Checklist" content:

```markdown
## Complete Prioritized TODO Example

```markdown
# Code Review TODO

**Generated:** 2026-04-20
**Repository:** my-project

## Code Quality

- [ ] **[High]** Handle unhandled promise rejection in `parseConfig()` (`src/config.ts:42`)
  Impact: If config is malformed, the process will crash. Add try/catch or `.catch()`.
- [ ] **[Medium]** Standardize import styles across utility modules (`src/utils/*.ts`)
  Impact: Mixed styles make intent unclear and can cause accidental runtime imports.
- [ ] **[Low]** Remove debug `console.log` from server startup (`src/server.ts:87`)
  Impact: Noise in production logs. Gate behind debug flag or remove.

## Refactoring

- [ ] **[Medium]** Extract duplicated validation logic into shared utility (`src/auth.ts:30`, `src/api.ts:45`)
  Impact: Reduces maintenance burden and prevents divergent validation rules.
- [ ] **[Low]** Remove unused helper function (`src/helpers.ts:12`)
  Impact: Dead code adds noise and confusion for future maintainers.

## Documentation

- [ ] **[Low]** Add usage examples to README for CLI commands (`README.md`)
  Impact: New contributors won't know how to run the tool without examples.

## Security

- [ ] **[High]** Remove hardcoded API key from config (`src/config.ts:42`)
  Impact: Exposed secrets can be exploited. Move to environment variables.

## Test Coverage

- [ ] **[Medium]** Add tests for error paths in `parseConfig()` (`tests/app.test.ts`)
  Impact: Without error-path tests, regressions in failure handling won't be caught.
- [ ] **[Low]** Fix brittle assertion pattern in utils tests (`tests/utils.test.ts:8`)
  Impact: Brittle tests break on unrelated changes and reduce confidence in CI.
```
```

Ensure existing template content is preserved. The new examples should be additive.
  </action>
  <verify>
    <automated>grep -c "## Summary" skills/code-reviewer/assets/review-template.md</automated>
    <automated>grep -c "## Prioritized Issue Table" skills/code-reviewer/assets/review-template.md</automated>
    <automated>grep -c "Impact:" skills/code-reviewer/assets/todo-template.md</automated>
    <automated>grep -c "\*\*\[High\]\*\*" skills/code-reviewer/assets/todo-template.md</automated>
  </verify>
  <done>
    - review-template.md contains a complete example with Summary table and Prioritized Issue Table
    - todo-template.md contains a complete example with severity badges and impact notes
    - Existing template content is preserved
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill instructions → Generated report | Skill instructions define report structure; malformed instructions could produce unusable output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | output-contract.md | accept | Contains only formatting rules, no secrets |
| T-05-02 | Tampering | SKILL.md Phase 5 | mitigate | Quality checks in Phase 5 verify count consistency; executor validates structure |
</threat_model>

<verification>
1. output-contract.md has Summary and Prioritized Issue Table sections
2. SKILL.md Phase 5 references both new sections
3. Templates include complete examples
4. All existing content is preserved (no regressions)
5. grep verifies key strings present in each file
</verification>

<success_criteria>
- Output contract defines complete prioritized format (Summary table + Prioritized Issue Table)
- SKILL.md instructs the skill to generate both sections in the correct order
- Templates provide concrete examples executors can follow
- All files pass automated grep verification for required content
</success_criteria>

<output>
After completion, create `.planning/phases/05-actionable-outputs-&-release-readiness/05-01-SUMMARY.md`
</output>
